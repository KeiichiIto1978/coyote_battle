# Codex 開発指示

## 1. 基本姿勢

- やりとり、説明、ドキュメント、原則としてコードコメントは日本語で書く。
- 回答は結論を先に示し、その後に根拠、変更点、確認結果を簡潔に続ける。
- `docs/proposal.md` をプロダクトの目的と初期要件、`docs/specifications/` を現在の詳細仕様の一次情報とする。
- 要件が曖昧で実装結果が大きく変わる場合は、推測で仕様を確定せず確認する。
- 小さく検証可能な単位で変更し、無関係な修正を混ぜない。

## 2. アーキテクチャ

- Unity / C# で Android 向けアプリを開発する。
- ゲームルールは `Domain`、進行制御は `Application`、表示と入力は `Presentation` に分離する。
- `Domain` は可能な限り `MonoBehaviour`、シーン、端末 API に依存させない。
- ユーザーと NPC は共通の参加者モデルとして扱い、操作方法の差を上位層で表現する。
- 乱数、時刻、外部入出力は差し替え可能にし、テストの再現性を保つ。
- 仕様変更時は、必要に応じて `docs/` の関連文書も同じ変更で更新する。

## 3. TDD

- 原則として Red、Green、Refactor の順で開発する。
- 不具合修正では、最初に不具合を再現するテストを追加する。
- ゲームルールと NPC 判断は EditMode テストを優先する。
- Unity のシーンや表示連携は、必要な範囲を PlayMode テストで確認する。
- テスト名は `対象_条件_期待結果` の形式を基本とし、意図を日本語で読める名前にする。
- テストを通すためだけの本番コード分岐を作らない。

## 4. 命名規則

- C# ファイル名は、原則として主要な型名と一致させる。
- クラス、構造体、列挙型、インターフェース、メソッド、プロパティは `PascalCase` を使う。
- インターフェース名は `I` で始める（例: `IRandomSource`）。
- ローカル変数と引数は `camelCase` を使う。
- private フィールドは `_camelCase` を使う。
- 定数は `PascalCase` を使う。
- bool 値は `Is`、`Has`、`Can`、`Should` など、真偽が分かる名前を優先する。
- 名前空間は `CoyoteBattle.<Layer>[.<Feature>]` の形式にする。
- Unity のアセット、シーン、フォルダーは `PascalCase` を使う。
- 略語や一文字名を避け、ゲーム用語は `docs/proposal.md` の定義に合わせる。

## 5. ファイルとコンテキストの上限

- `AGENTS.md`、各 `SKILL.md` など AI が読む指示文書は1ファイル200行以内にする。
- C# などのソースコードは、生成コードを除き1ファイル500行以内にする。
- 上限に近づいたら責務を見直して分割する。単なる行数回避の分割はしない。
- 1ファイルには原則1つの主要な型を置く。

## 6. コード品質

- `.editorconfig` とプロジェクトの formatter 設定に従う。
- 変更後は対象テスト、可能なら全テストを実行する。
- lint、コンパイル警告、テスト失敗を放置しない。対応できない場合は理由を報告する。
- nullable、安全な境界値、無効な状態を意識し、入力値を適切に検証する。
- コードには、意図、前提、判断理由、注意点が読み取れるよう、必要なコメントを適宜記載する。
- 関数とメソッドには、入力、出力、主要な処理が分かるドキュメントコメントを記載する。戻り値がない場合も副作用や状態変更を明記する。
- 自明なコードを逐語的に説明せず、コメントと実装が重複しすぎないようにする。
- テストコードには、何を保証するテストか、またはどの不具合を防ぐテストかが分かるコメントを記載する。
- 公開 API を増やしすぎず、変更可能範囲を小さく保つ。

## 7. 仕様管理

- Issueを要望、議論、詳細設計、テストケース、受け入れ条件、未確定事項をリファインメントで決める場所とする。
- リポジトリ内ドキュメントは、Issue単位ではなく機能や関心事単位で整理し、現時点の正しい仕様を記述する。
- 仕様の追加・変更を含むPRでは、Issueの合意事項を`docs/specifications/` の既存文書へ整理して転記するか、適切な文書を追加する。
- 実装中に追加で決まった制約、例外、技術的補足、テストケースもリポジトリ内文書へ反映する。
- 長期的に参照する重要な設計判断と理由は `docs/decisions/` にADRとして残す。日常的な実装詳細はADRにしない。
- Issueで合意した内容をドキュメントへ反映する際、別途承認は求めない。コードと同様にPRレビューで確認を受ける。
- Issueと文書が矛盾する場合、作業中は対象Issueの最新合意を優先し、マージ前に文書を整合させる。マージ後はリポジトリ内文書を現在仕様として扱う。
- 議論の履歴を丸ごと複製せず、Issueで決まった結論と、利用者・実装者が必要な詳細を現在形で残す。

## 8. IssueとPR

- 機能、仕様、振る舞い、公開API、依存関係、ビルド設定を変更するPRは、原則として対応Issueを1件以上持つ。
- PR本文に `Closes #<Issue番号>` または `Refs #<Issue番号>` を記載し、対応関係を明示する。完了させるPRでは `Closes` を使う。
- 実装開始前に対象Issueと受け入れ条件を確認する。Issueがなく例外にも該当しない場合は、Issueの作成または指定を依頼する。
- Issueなしを許容するのは、振る舞いに影響しないタイポ、表記、リンク、書式、コメントのみの小さな修正に限る。
- IssueなしのPRでは、本文に `Issueなし: 軽微変更` と理由を記載する。作業中に例外範囲を超えた場合はIssueを作成して関連付ける。
- リファクタリング、テスト追加、依存関係更新、設定変更は影響評価が必要なため、軽微な例外に含めない。
- 複数Issueを1つのPRに含めるのは、変更を安全に分離できない場合に限り、その理由をPR本文に記載する。

## 9. 完了報告

- 結論、主な変更、実施した確認、残課題の順で報告する。
- テストを実行できなかった場合は、未実施であることと理由を明記する。
- 企画書にない仕様を仮定した場合は、その仮定を明記する。
